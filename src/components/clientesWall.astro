---
const technologies = [
  { name: "Junta de Andalucía", color: "#5EAD93" },
  { name: "Maristas", color: "#FF8AB5" },
  { name: "Universidad de Jaén", color: "#5EAD93" },
  { name: "Veracruz", color: "#FE5F55" },
  { name: "Ayuntamiento de Jaén", color: "#FBAF00" },
  { name: "Divino Maestro", color: "#00A6D3" },
  { name: "Vandelvira", color: "#FBAF00" }
];
---
<div class="relative overflow-x-hidden mb-2">
  <div
    class="pointer-events-none absolute inset-y-0 left-0 w-32 bg-gradient-to-r from-slate-100 to-transparent z-20"
  ></div>
  <div
    class="pointer-events-none absolute inset-y-0 right-0 w-32 bg-gradient-to-l from-slate-100 to-transparent z-20"
  ></div>

  <div class="flex animate-scroll hover:animate-paused gap-12 md:gap-20 w-max">
    {
      [...technologies, ...technologies, ...technologies].map((tech) => (
        <div class="flex items-center gap-2 group transition-all duration-300">
          <span class="text-lg font-medium" style={`color: ${tech.color};`}>
            {tech.name}
          </span>
        </div>
      ))
    }
  </div>
</div>

<style is:global>
  @keyframes scroll {
    from {
      transform: translateX(0);
    }
    to {
      transform: translateX(-50%);
    }
  }
  .animate-scroll {
    animation: scroll 60s linear infinite;
  }

  @media (min-width: 768px) {
    .animate-scroll {
      animation-duration: 50s;
    }
  }
</style>
